# Database Configuration
# PostgreSQL connection settings
DB_NAME=clubcentric_db
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here
DB_HOST=localhost
DB_PORT=5432

# API Configuration (optional)
# API URL prefix for endpoints
# If not set, URLs will be /admin/, /clubs/, etc. (no prefix)
# If set to "api", URLs will be /api/admin/, /api/clubs/, etc.
API_URL=api

